let c;
let dp;

var f = function (amount) {
    if (amount < 0) return Number.MAX_SAFE_INTEGER;
    if (amount === 0) return 0;
    if (dp[amount] != -1) return dp[amount];

    let ans = Number.MAX_SAFE_INTEGER;
    for()
}

var coinChange = function (amount, coins) {
    
}