var asteroidCollision = function (a) {
    let st = new Stack();
    let i = 0;
    while(i < a.length) {
        if(st.isEmpty()) {
            st.push(arr[i]);
            i++;
        } else {
            if () {}
        }
    }
}